node_modules/
temporary screenshots/
.cursor/skills/
.claude/

# Secrets — never commit (set in Vercel → Environment Variables or local .env)
.env
.env.*
!.env.example

# Generated by postinstall — same as api/ (needed when Vercel root directory is cra/)
cra/api/
