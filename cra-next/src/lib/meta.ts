// Meta Pixel ID — public identifier, safe to hardcode (it's in page HTML).
// Hardcoded here so swapping Pixels is a code change with a paper trail,
// not a silent Vercel env var edit. Update this constant + redeploy to swap.
export const META_PIXEL_ID = "939105465792094";
