# Copy to .env for local dev only. Do not commit real values.
# Production: set in Vercel → Settings → Environment Variables

ANTHROPIC_API_KEY=
# Optional — e.g. claude-sonnet-4-6 (default if unset)
ANTHROPIC_MODEL=
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=
